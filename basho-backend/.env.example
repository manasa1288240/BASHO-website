# Copy to .env and set your values
MONGODB_URI=mongodb://localhost:27017/basho
PORT=5000
# JWT secret for signing auth tokens
JWT_SECRET=your_jwt_secret_here

# (Optional) Admin account password-based login uses the `isAdmin` flag on a user.